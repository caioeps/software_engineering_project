<%= form_for(@report) do |f| %>
  <% if @report.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@report.errors.count, "error") %> prohibited this report from being saved:</h2>

      <ul>
      <% @report.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :text %><br>
    <%= f.text_area :text %>
  </div>
  <div class="field">
    <%= f.label :occurrence_id_id %><br>
    <%= f.text_field :occurrence_id_id %>
  </div>
  <div class="field">
    <%= f.label :comment_id_id %><br>
    <%= f.text_field :comment_id_id %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
